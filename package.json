{
  "name": "todo_list02",
  "version": "1.0.0",
  "description": "My stunning Nuxt.js project",
  "author": "ktg19",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --verdose"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.0.0",
    "vue2-touch-events": "^2.2.1"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.0.0",
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1"
  }
}
